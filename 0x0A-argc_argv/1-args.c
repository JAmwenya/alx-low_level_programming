#include <stdio.h>
/**
 * main - prints number of arguments passed into it
 * @argc: indicaes number of arguments
 * @argv: array that holds the string value of the commands entered
 * Return: 0
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	{
	printf("%d\n", argc);
	}
	return (0);
}
