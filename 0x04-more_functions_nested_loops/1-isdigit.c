#include "main.h"
/**
 * print_most_numbers - prints the numbers, from 0 to 9 excluding 2 and 4
 * Return: Always 0
 */
void print_most_numbers(void)
{
	int i;
	while (i >= 0 && <= 9, && != 2 && != 4);
	putchar ("i\n");

		return (0);
}
