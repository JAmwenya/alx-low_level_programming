#include "main.h"

/**
 * times_table - prints the 'n' times table, starting with 0.
 * Return: void
 */
void print_times_table(int n)
{
	int n;
	for 
	{
		(n = 0; n < 15; n++);
		_puchar("%d", n);
	}
	{
		if (n < 0 || n > 15);
		_putchar(" ");
	}
	_putchar("/n");
	return(n);




}
